git filter-branch -f --commit-filter '
        if [ "$GIT_COMMITTER_NAME" = "Ben March" ];
        then
                GIT_COMMITTER_EMAIL="bmarch89@gmail.com";
                GIT_AUTHOR_EMAIL="bmarch89@gmail.com";
                git commit-tree "$@";
        else
                git commit-tree "$@";
        fi' HEAD
